<%= form_for(:session, url: root_path) do |f| %>
  <%= f.submit "Search", type: :image, src: image_path("search.png"), alt: "search" %>
  <%= f.text_field :text, placeholder: "Find a bed" %>
<% end %>